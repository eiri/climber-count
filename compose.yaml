services:
  climber-count:
    image: ghcr.io/eiri/climber-count
    env_file: ".env"
    ports:
      - "9090:9090"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - ./storage:/storage
